<Window x:Class="PharmacyApp.Views.PrintPriceTagsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Печать ценников" Height="550" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите товары для печати ценников" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Категория:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="Name" 
                      SelectedItem="{Binding SelectedCategory}" Width="200" Margin="0,0,10,0"/>
            <Button Content="Фильтровать" Command="{Binding FilterCommand}" Padding="10,5"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding FilteredItems}" SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Предварительный просмотр" Command="{Binding PreviewCommand}" 
                    Width="150" Margin="0,0,10,0"/>
            <Button Content="Печать" Command="{Binding PrintCommand}" 
                    Width="100" Margin="0,0,10,0" Background="#2C7BE5" Foreground="White"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>