<Window x:Class="PharmacyApp.Views.PriceMarkupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление наценки на категорию" Height="450" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите категорию:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TreeView Grid.Row="1" ItemsSource="{Binding Categories}" 
                  SelectedItemChanged="TreeView_SelectedItemChanged"
                  Margin="0,0,0,10">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding ChildCategories}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <TextBlock Grid.Row="2" Text="Процент наценки (%):" FontWeight="SemiBold" Margin="0,5,0,5"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBox Text="{Binding MarkupPercent, UpdateSourceTrigger=PropertyChanged}" 
                     Width="100" Margin="0,0,10,0"/>
            <TextBlock Text="(положительное – увеличение, отрицательное – снижение)" FontSize="11" VerticalAlignment="Center"/>
        </StackPanel>

        <CheckBox Grid.Row="4" Content="Учитывать предельные наценки для ЖНВЛП" 
                  IsChecked="{Binding RespectVitalLimits}" Margin="0,10,0,15"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Применить" Command="{Binding ApplyMarkupCommand}" 
                    Width="100" Margin="0,0,10,0" Background="#2C7BE5" Foreground="White"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>